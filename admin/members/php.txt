# 문서 내에 php라는 언어가 하나라도 있다면 .html이 아니라 .php로 저장한다.
# php는 선언문이 $이다.
# include or require
# vscode 기준으로 ./는 우리가 열어놓은 최상위 폴더가 된다. > 근데 수업 중에는 ./가 web_project가 잡히네 그래서 ./를 사용하면 루트를 정할 때 주소를 일괄적으로 관리할 수 있음.
# get : 주소에 데이터 포함하여 전송(http://domain?변수=값) >> pager쪽에는 이 방식을 사용한다.


---------
